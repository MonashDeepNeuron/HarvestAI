# Core
ultralytics>=8.4.132  # strawberry_v2 is a YOLO26-seg model; older builds can't load it
opencv-python>=4.10.0
numpy>=1.26.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0

# UI (perception/ui)
gradio>=4.44.0
pillow>=10.0.0
